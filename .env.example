# 小说分析器配置文件
# 复制此文件为 .env 并修改以下配置

# 小说根目录（必须配置）
# 示例：NOVEL_PATH=X:\Gallery\h小说
NOVEL_PATH=X:\Gallery\h小说

# OpenAI 兼容 API 配置（必须配置）
# API 地址：完整的 OpenAI 兼容 API 地址
# 示例：API_BASE_URL=https://api.openai.com/v1
API_BASE_URL=https://your-api.com/v1

# API 密钥：您的 API Key
# 示例：API_KEY=sk-proj-xxxxxxxxxxxx
API_KEY=sk-your-api-key

# 模型名称：要使用的 LLM 模型
# 示例：MODEL_NAME=gpt-4o, gpt-4o-mini, claude-3-5-sonnet
MODEL_NAME=gpt-4o

# 服务器监听地址（默认仅本机，安全性较高）
# 127.0.0.1 = 仅本机访问（推荐）
# 0.0.0.0 = 局域网可访问（存在安全风险）
HOST=127.0.0.1

# 服务器端口
PORT=6103

# 日志级别：debug, info, warning, error, critical
LOG_LEVEL=warning

# 调试模式：是否显示 LLM 原始响应
# 生产环境建议设置为 false
DEBUG=false

LLM_TEMPERATURE_STRUCTURED=0.2
LLM_USE_FUNCTION_CALLING=false
LLM_CONTENT_MAX_CHARS=24000
LLM_CONTENT_STRATEGY=head_middle_tail
LLM_REPAIR_ENABLED=true
LLM_REPAIR_PROMPT_HEAD_MAX_CHARS=8000
LLM_REPAIR_BAD_OUTPUT_MAX_CHARS=6000
